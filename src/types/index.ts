export enum STYLE_TYPE {
  FILL = 'FILL',
  TEXT = 'TEXT',
  EFFECT = 'EFFECT',
  GRID = 'GRID',
}

export interface INodesArray {
  key: string;
  value: string;
}

export enum FILE_TYPE {
  SVG = 'svg',
  PNG = 'png',
  JPG = 'jpg',
  PDF = 'pdf',
}
